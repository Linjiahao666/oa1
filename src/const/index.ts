export enum MESSAGETYPE {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  FILE = 'file',
}

export enum EditorType {
  ASYNCEDITOR = 'AsyncEditor',
  VUEEDITOR = 'VueEditor',
}

export const DrawConfig = {
  width: 100,
  height: 30,
}